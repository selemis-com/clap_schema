{
  "operations": [
    {
      "path": [
        "get"
      ],
      "output": {
        "$schema": "https://json-schema.org/draft/2020-12/schema",
        "description": "Resource returned by the get command.",
        "properties": {
          "id": {
            "description": "Stable resource identifier.",
            "type": "string"
          },
          "name": {
            "description": "Human-readable resource name.",
            "type": "string"
          }
        },
        "required": [
          "id",
          "name"
        ],
        "title": "Resource",
        "type": "object"
      }
    }
  ]
}
