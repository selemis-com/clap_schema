{
  "name": "agentctl",
  "path": [],
  "description": "Top-level arguments for the agent-control CLI",
  "usage": "agentctl [OPTIONS] <COMMAND>",
  "options": [
    {
      "id": "json",
      "long": "json",
      "help": "Emits command results as JSON when enabled"
    }
  ],
  "has_subcommands": true,
  "subcommands": [
    {
      "name": "get",
      "path": [
        "get"
      ],
      "description": "Show one resource",
      "usage": "agentctl get [OPTIONS] <ID>",
      "arguments": [
        {
          "id": "id",
          "index": 1,
          "value_names": [
            "ID"
          ],
          "help": "Identifier of the resource to fetch",
          "required": true
        }
      ],
      "options": [
        {
          "id": "json",
          "long": "json",
          "help": "Emits command results as JSON when enabled"
        }
      ],
      "executable": true,
      "output": {
        "description": "Resource returned by the get command.",
        "properties": {
          "id": {
            "description": "Stable resource identifier.",
            "type": "string"
          },
          "name": {
            "description": "Human-readable resource name.",
            "type": "string"
          }
        },
        "required": [
          "id",
          "name"
        ],
        "type": "object"
      }
    },
    {
      "name": "schema",
      "path": [
        "schema"
      ],
      "description": "Discover commands and successful-output contracts",
      "usage": "agentctl schema [OPTIONS] [PATH]...",
      "arguments": [
        {
          "id": "path",
          "index": 1,
          "value_names": [
            "PATH"
          ],
          "help": "Optional command path, such as `get`"
        }
      ],
      "options": [
        {
          "id": "full",
          "long": "full",
          "help": "Recursively resolve visible child commands"
        },
        {
          "id": "json",
          "long": "json",
          "help": "Emits command results as JSON when enabled"
        }
      ],
      "executable": true,
      "output": {
        "$defs": {
          "ArgumentInfo": {
            "description": "Compact context for one visible Clap argument or option./n/nThis intentionally exposes only straightforward facts from Clap's built command model./nComplete invocation semantics remain authoritative in Clap and its generated help.",
            "properties": {
              "aliases": {
                "description": "Long aliases that Clap exposes in generated help.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "default_values": {
                "description": "Visible UTF-8 default values for value-taking arguments.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "help": {
                "description": "Human-readable argument help reflected from Clap.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "id": {
                "description": "Clap argument identifier.",
                "type": "string"
              },
              "index": {
                "description": "Positional index when this is a positional argument.",
                "format": "uint",
                "minimum": 0,
                "type": [
                  "integer",
                  "null"
                ]
              },
              "long": {
                "description": "Long option name when configured.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "possible_values": {
                "description": "Visible finite values reported by the configured Clap value parser.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              },
              "required": {
                "description": "Whether Clap marks this argument as unconditionally required.",
                "type": "boolean"
              },
              "short": {
                "description": "Short option name when configured.",
                "maxLength": 1,
                "minLength": 1,
                "type": [
                  "string",
                  "null"
                ]
              },
              "short_aliases": {
                "description": "Short aliases that Clap exposes in generated help.",
                "items": {
                  "maxLength": 1,
                  "minLength": 1,
                  "type": "string"
                },
                "type": "array"
              },
              "value_names": {
                "description": "Value placeholders such as `FILE` or `WORKSPACE_ID`.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            },
            "required": [
              "id"
            ],
            "type": "object"
          },
          "SchemaCommandSummary": {
            "description": "Compact schema-discovery reference to one direct child command.",
            "properties": {
              "description": {
                "description": "Command description reflected from Clap help metadata.",
                "type": [
                  "string",
                  "null"
                ]
              },
              "executable": {
                "description": "Whether this command is executable.",
                "type": "boolean"
              },
              "has_subcommands": {
                "description": "Whether this command has schema-visible child commands.",
                "type": "boolean"
              },
              "path": {
                "description": "Canonical command path excluding the executable name.",
                "items": {
                  "type": "string"
                },
                "type": "array"
              }
            },
            "required": [
              "path"
            ],
            "type": "object"
          },
          "SchemaSubcommand": {
            "anyOf": [
              {
                "$ref": "#/$defs/SchemaCommandSummary",
                "description": "Compact child reference used by shallow schema discovery."
              },
              {
                "$ref": "#",
                "description": "Recursively resolved child used by full schema discovery."
              }
            ],
            "description": "One child entry in a [`SchemaDocument`]."
          }
        },
        "description": "Resolved schema-discovery document for one selected command./n/nThe selected command is flattened into the document itself. In shallow mode, `subcommands`/ncontains [`SchemaCommandSummary`] entries for direct children. In full mode, each child is/nanother fully resolved `SchemaDocument`. The wire shape is therefore stable while `--full`/nchanges only the amount of child detail returned.",
        "properties": {
          "aliases": {
            "description": "Aliases that Clap exposes in generated help.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "arguments": {
            "description": "Visible positional arguments reflected directly from Clap.",
            "items": {
              "$ref": "#/$defs/ArgumentInfo"
            },
            "type": "array"
          },
          "description": {
            "description": "Command description reflected from Clap help metadata.",
            "type": [
              "string",
              "null"
            ]
          },
          "executable": {
            "description": "Whether this node is executable.",
            "type": "boolean"
          },
          "has_subcommands": {
            "description": "Whether the node has schema-visible child commands.",
            "type": "boolean"
          },
          "name": {
            "description": "Canonical command name.",
            "type": "string"
          },
          "options": {
            "description": "Visible non-positional options reflected directly from Clap.",
            "items": {
              "$ref": "#/$defs/ArgumentInfo"
            },
            "type": "array"
          },
          "output": {
            "description": "Successful output schema when the command returns a non-unit value."
          },
          "path": {
            "description": "Canonical path excluding the executable name.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "subcommands": {
            "description": "Direct child commands at the requested resolution depth.",
            "items": {
              "$ref": "#/$defs/SchemaSubcommand"
            },
            "type": "array"
          },
          "usage": {
            "description": "Invocation synopsis rendered by Clap./n/nThis is presentation output, not a structured grammar; Clap may collapse groups of options/nbehind placeholders such as `[OPTIONS]`.",
            "type": "string"
          }
        },
        "required": [
          "name",
          "path",
          "usage"
        ],
        "type": "object"
      }
    }
  ]
}
